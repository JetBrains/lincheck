=== Loop Information ===
LOOP 1
  HEADER: B0
  BODY: B0, B1, B2, B3, B4, B5
  BACK EDGES:
    B4 -> B0 : JUMP(opcode=GOTO)
    B5 -> B0 : JUMP(opcode=GOTO)
  NORMAL EXITS:
    B0 -> B6 : JUMP(opcode=IFNE)
  EXCEPTION EXIT HANDLERS:
    NONE
  EXCLUSIVE EXITS:
    B6
LOOP 2
  HEADER: B1
  BODY: B1, B2
  BACK EDGES:
    B2 -> B1 : JUMP(opcode=GOTO)
  NORMAL EXITS:
    B1 -> B3 : JUMP(opcode=IFNE)
  EXCEPTION EXIT HANDLERS:
    NONE
  EXCLUSIVE EXITS:
    B3


=== Await Path Detection ===
No await paths detected.