=== Loop Information ===
LOOP 1
  HEADER: B0
  BODY: B0, B1, B2, B3, B4, B5
  BACK EDGES:
    B2 -> B0 : JUMP(opcode=GOTO)
    B5 -> B0 : JUMP(opcode=GOTO)
  NORMAL EXITS:
    B0 -> B6 : JUMP(opcode=IFNE)
  EXCEPTION EXIT HANDLERS:
    NONE
  EXCLUSIVE EXITS:
    B6
LOOP 2
  HEADER: B3
  BODY: B3, B4
  BACK EDGES:
    B4 -> B3 : JUMP(opcode=GOTO)
  NORMAL EXITS:
    B3 -> B5 : JUMP(opcode=IFNE)
  EXCEPTION EXIT HANDLERS:
    NONE
  EXCLUSIVE EXITS:
    B5


=== Await Path Detection ===
Await loop 1: header=B0, body=[B0, B1, B2, B3, B4, B5], cleanBackEdgeSources=[B2]